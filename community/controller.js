'use strict';

/* In the future, this code should contain code to fetch and display data such as
 * "this user is currently doing this task", "this user validated this task", etc.
 * as in the current http://www.france-ioi.org/ website.
 */

angular.module('algorea')
   .controller('communityController', ['$scope', function ($scope) {
      $scope.events = [
      ];
}]);
